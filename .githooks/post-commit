#!/bin/sh

# GitHub Actions is the only automatic Drive writer, which prevents local
# commits and pushed commits from racing one another.
branch=$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)
if [ "$branch" != "main" ]; then
  exit 0
fi

echo "Drive Current will update after this main commit is pushed to GitHub."
